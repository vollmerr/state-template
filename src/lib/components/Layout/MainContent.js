import React from 'react';

import LayoutBase from './LayoutBase';

const MainContent = (props) => (
  <LayoutBase baseClass={'main-content'} id={'main-content'} {...props} />
);

export default MainContent;
